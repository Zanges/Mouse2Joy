<Window x:Class="Mouse2Joy.UI.Views.OverlayWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Mouse2Joy Overlay"
        WindowStyle="None"
        AllowsTransparency="True"
        Background="Transparent"
        Topmost="True"
        ShowInTaskbar="False"
        ResizeMode="NoResize"
        SnapsToDevicePixels="True"
        UseLayoutRounding="True">
    <!-- Permanently click-through. The hairline alpha on HostCanvas is a relic of
         the old configure-mode drag — leaving the canvas with a fully transparent
         background is fine because WS_EX_TRANSPARENT (set in code-behind) makes
         the whole window non-hit-testable from the OS. -->
    <Canvas x:Name="HostCanvas" Background="Transparent"/>
</Window>
